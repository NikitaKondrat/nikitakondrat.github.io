on:
  push:
    workflows: deploy-site
    filter:
      branches: main
      paths: docs/**

workflows:
  deploy-site:
    tasks:
      - name: deploy
        cubes:
          - name: push-to-sourcecraft
            image: docker.io/library/alpine/git
            script:
              - git push sourcecraft