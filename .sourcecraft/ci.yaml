on:
  push:
    workflows: deploy-site
    filter:
      branches: main
      paths: docs/**

workflows:
  deploy-site:
    tasks:
      - name: deploy
        cubes:
          - name: push-to-sourcecraft
            script:
              - git push sourcecraft