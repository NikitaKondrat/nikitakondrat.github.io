on:
  push:
    - workflows: deploy-site
      filter:
        branches: ["main", "master"]
        paths: ["docs/**"]

workflows:
  deploy-site:
    tasks:
      - name: deploy
        script:
          - git push sourcecraft