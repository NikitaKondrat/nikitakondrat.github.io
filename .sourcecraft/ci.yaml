on:
  push:
    workflows: build-site
    filter:
      branches: main

workflows:
  build-site:
    tasks:
      - name: build-and-publish-site
        cubes:
          - name: publish-to-release-branch
            script:
              - git checkout -B main
              - git add ./docs
              - "git commit -m \"feat: автоматическое обновление сайта\" || true"
              - "git push origin main -f"